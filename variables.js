'use strict';

export.model{
    function docente(model){
      return docente[].name;
      model.teacherName
    }
}
export.model.docente = function docente(model){
  return docente[].description;
  model.teacherName
}
